const assert = require("assert")  //require importa un modulo di Node (al posto di import)
//cercare se possibile installare assert via NPM

function convertitore(p1, p2, p3){
    return 0;

}

//insieme di test
describe("test fixture", () => {

    it("testName1", () => {
        assert.equal(1, 1)
    })

} );   
